import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  align-items: center;
  padding: 1.5rem 1rem;
`;

export const Title = styled.h2`
  cursor: pointer;
  color: var(--primary);
`;
